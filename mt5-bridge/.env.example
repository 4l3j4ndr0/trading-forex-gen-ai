# Bridge
BRIDGE_PORT=5000
BRIDGE_API_KEY=

# Database (same connection string as backend)
DATABASE_URL=

# App Key (same APP_KEY as backend — for decrypting broker passwords)
APP_KEY=

# MT5 Path
MT5_PATH=C:\Program Files\XM Global MT5\terminal64.exe

# Security (comma-separated IPs allowed, empty = allow all)
ALLOWED_IPS=
